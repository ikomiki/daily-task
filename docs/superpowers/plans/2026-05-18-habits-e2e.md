# M12 E2E / CI（Playwright + GH Actions）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Playwright E2E テストを `/habits` アプリに導入し、GitHub Actions CI で Supabase ローカルを起動して E2E が自動実行されるようにする。

**Architecture:** `apps/habits/playwright.config.ts` を中心に、`setup` プロジェクト（`auth.setup.ts`）が認証済み storageState を生成し、`authenticated` プロジェクトがそれを再利用する。`unauthenticated` プロジェクトは認証フローと full journey テストを実行する。CI では `supabase/setup-cli` で Docker ベースの Supabase を起動してから E2E を実行する。

**Tech Stack:** `@playwright/test ^1.50.0`、`@nx/playwright` v22.7.0、Supabase ローカル（Docker）、`supabase/setup-cli` GitHub Action、Vite dev server（port 5173）

---

## ファイル構成

| 操作 | パス | 責務 |
|---|---|---|
| Create | `apps/habits/playwright.config.ts` | Playwright 設定（webServer・3 プロジェクト）|
| Create | `apps/habits/e2e/auth.setup.ts` | テストユーザー作成 + storageState 保存 |
| Create | `apps/habits/e2e/auth.spec.ts` | 認証フロー E2E（unauthenticated） |
| Create | `apps/habits/e2e/signup-journey.spec.ts` | サインアップ→完了→stash 反映 E2E |
| Create | `apps/habits/e2e/offline-sync.spec.ts` | オフライン→再接続→同期 E2E |
| Create | `apps/habits/e2e/.gitignore` | `.auth/` と成果物を git 除外 |
| Modify | `apps/habits/package.json` | `@playwright/test: "catalog:"` を devDeps に追加 |
| Modify | `apps/habits/tsconfig.json` | `playwright.config.ts` と `e2e/**/*.ts` を include に追加 |
| Modify | `.github/workflows/ci.yml` | Supabase セットアップステップを追加 |

---

### Task 1: Playwright 設定ファイルの作成

**Files:**
- Create: `apps/habits/playwright.config.ts`
- Create: `apps/habits/e2e/.gitignore`
- Modify: `apps/habits/package.json`
- Modify: `apps/habits/tsconfig.json`

- [ ] **Step 1: `@playwright/test` を依存に追加する**

`apps/habits/package.json` の `devDependencies` に追加（`@supabase/supabase-js` の後）:

```json
{
  "devDependencies": {
    "@org/config-biome": "workspace:*",
    "@org/config-tailwind": "workspace:*",
    "@org/config-tsconfig": "workspace:*",
    "@org/config-vitest": "workspace:*",
    "@playwright/test": "catalog:",
    "@tailwindcss/vite": "catalog:",
    "@testing-library/jest-dom": "catalog:",
    "@testing-library/react": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "jsdom": "catalog:",
    "vite": "catalog:",
    "vite-plugin-pwa": "catalog:",
    "vitest": "catalog:",
    "workbox-window": "catalog:"
  }
}
```

- [ ] **Step 2: `tsconfig.json` に e2e ファイルを追加する**

`apps/habits/tsconfig.json` を以下に更新:

```json
{
  "extends": "@org/config-tsconfig/app.json",
  "compilerOptions": {
    "types": ["vite/client"]
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "vite.config.ts",
    "playwright.config.ts",
    "e2e/**/*.ts"
  ]
}
```

- [ ] **Step 3: `playwright.config.ts` を作成する**

```ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env['CI'],
  retries: process.env['CI'] ? 2 : 0,
  workers: process.env['CI'] ? 1 : undefined,
  reporter: [['html', { open: 'never' }]],
  use: {
    baseURL: process.env['BASE_URL'] ?? 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'setup',
      testMatch: '**/*.setup.ts',
    },
    {
      name: 'authenticated',
      use: {
        ...devices['Desktop Chrome'],
        storageState: 'e2e/.auth/user.json',
      },
      dependencies: ['setup'],
      testIgnore: ['**/*.setup.ts', '**/auth.spec.ts', '**/signup-journey.spec.ts'],
    },
    {
      name: 'unauthenticated',
      use: { ...devices['Desktop Chrome'] },
      testMatch: ['**/auth.spec.ts', '**/signup-journey.spec.ts'],
    },
  ],
  webServer: {
    command: 'pnpm nx serve habits',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env['CI'],
    timeout: 120_000,
  },
});
```

- [ ] **Step 4: `e2e/.gitignore` を作成する**

```
.auth/
playwright-report/
test-results/
```

- [ ] **Step 5: `pnpm install` を実行して依存を解決する**

```bash
pnpm install
```

Expected: `@playwright/test` がインストールされる

- [ ] **Step 6: TypeScript が通ることを確認する**

```bash
CI=true pnpm nx typecheck habits
```

Expected: PASS（エラーなし）

- [ ] **Step 7: Commit する**

```bash
git add apps/habits/playwright.config.ts apps/habits/e2e/.gitignore apps/habits/package.json apps/habits/tsconfig.json pnpm-lock.yaml
git commit -m "feat(e2e): Playwright 設定ファイルと tsconfig 設定を追加する"
```

---

### Task 2: テストユーザー作成（`auth.setup.ts`）

**Files:**
- Create: `apps/habits/e2e/auth.setup.ts`

- [ ] **Step 1: `auth.setup.ts` を作成する**

```ts
import { expect, test as setup } from '@playwright/test';

const authFile = 'e2e/.auth/user.json';
const email = process.env['E2E_TEST_EMAIL'] ?? 'e2e@habits.test';
const password = process.env['E2E_TEST_PASSWORD'] ?? 'habits-e2e-pw';

setup('テストユーザーでログイン済み状態を保存する', async ({ page }) => {
  // サインアップを試みる（既存ユーザーの場合は /today 以外に留まる）
  await page.goto('/auth/signup');
  await page.getByLabel('メールアドレス').fill(email);
  await page.getByLabel('パスワード').fill(password);
  await page.getByRole('button', { name: '新規登録' }).click();

  const redirected = await page
    .waitForURL('**/today', { timeout: 5000 })
    .then(() => true)
    .catch(() => false);

  if (!redirected) {
    // ユーザーが既に存在する場合はログインにフォールバック
    await page.goto('/auth/login');
    await page.getByLabel('メールアドレス').fill(email);
    await page.getByLabel('パスワード').fill(password);
    await page.getByRole('button', { name: 'ログイン' }).click();
    await page.waitForURL('**/today');
  }

  await expect(page.getByRole('heading', { name: '今日のタスク' })).toBeVisible();
  await page.context().storageState({ path: authFile });
});
```

- [ ] **Step 2: `.auth/` ディレクトリの存在確認（storageState 保存先）**

Playwright が自動的にディレクトリを作成するため事前作成は不要。ただし `.gitignore` で除外済みであることを確認する。

```bash
grep '.auth' apps/habits/e2e/.gitignore
```

Expected: `.auth/` が出力される

- [ ] **Step 3: setup プロジェクトのみ実行してセッションが保存されることを確認する**

※ Supabase ローカルが起動済みであること（`supabase start`）

```bash
pnpm nx e2e habits -- --project=setup
```

Expected: PASS + `apps/habits/e2e/.auth/user.json` が生成される

- [ ] **Step 4: Commit する**

```bash
git add apps/habits/e2e/auth.setup.ts
git commit -m "feat(e2e): auth.setup.ts — テストユーザー作成と storageState 保存"
```

---

### Task 3: 認証フロー E2E テスト（`auth.spec.ts`）

**Files:**
- Create: `apps/habits/e2e/auth.spec.ts`

- [ ] **Step 1: `auth.spec.ts` を作成する（テスト先行）**

```ts
import { expect, test } from '@playwright/test';

const email = process.env['E2E_TEST_EMAIL'] ?? 'e2e@habits.test';
const password = process.env['E2E_TEST_PASSWORD'] ?? 'habits-e2e-pw';

test('/auth/login でログインページが表示される', async ({ page }) => {
  await page.goto('/auth/login');
  await expect(page.getByRole('heading', { name: 'ログイン' })).toBeVisible();
  await expect(page.getByLabel('メールアドレス')).toBeVisible();
  await expect(page.getByLabel('パスワード')).toBeVisible();
  await expect(page.getByRole('button', { name: 'ログイン' })).toBeVisible();
});

test('/auth/signup で新規登録ページが表示される', async ({ page }) => {
  await page.goto('/auth/signup');
  await expect(page.getByRole('heading', { name: '新規登録' })).toBeVisible();
  await expect(page.getByRole('button', { name: '新規登録' })).toBeVisible();
});

test('未認証で /today にアクセスすると /auth/login にリダイレクトされる', async ({ page }) => {
  await page.goto('/today');
  await page.waitForURL('**/auth/login');
  await expect(page.getByRole('heading', { name: 'ログイン' })).toBeVisible();
});

test('未認証で / にアクセスすると /auth/login にリダイレクトされる', async ({ page }) => {
  await page.goto('/');
  await page.waitForURL('**/auth/login');
  await expect(page.getByRole('heading', { name: 'ログイン' })).toBeVisible();
});

test('無効なパスワードでログインするとエラーメッセージが表示される', async ({ page }) => {
  await page.goto('/auth/login');
  await page.getByLabel('メールアドレス').fill(email);
  await page.getByLabel('パスワード').fill('wrongpassword123');
  await page.getByRole('button', { name: 'ログイン' }).click();
  await expect(page.getByRole('alert')).toBeVisible();
});

test('正しい認証情報でログインすると /today にリダイレクトされ見出しが表示される', async ({
  page,
}) => {
  await page.goto('/auth/login');
  await page.getByLabel('メールアドレス').fill(email);
  await page.getByLabel('パスワード').fill(password);
  await page.getByRole('button', { name: 'ログイン' }).click();
  await page.waitForURL('**/today');
  await expect(page.getByRole('heading', { name: '今日のタスク' })).toBeVisible();
});

test('/today でログアウトすると /auth/login にリダイレクトされる', async ({ page }) => {
  // ログイン
  await page.goto('/auth/login');
  await page.getByLabel('メールアドレス').fill(email);
  await page.getByLabel('パスワード').fill(password);
  await page.getByRole('button', { name: 'ログイン' }).click();
  await page.waitForURL('**/today');

  // ログアウト
  await page.getByRole('button', { name: 'ログアウト' }).click();
  await page.waitForURL('**/auth/login');
  await expect(page.getByRole('heading', { name: 'ログイン' })).toBeVisible();
});
```

- [ ] **Step 2: unauthenticated プロジェクトでテストを実行する**

※ `auth.setup.ts` を先に実行済みか、`--project=unauthenticated` で直接実行する

```bash
pnpm nx e2e habits -- --project=unauthenticated --grep "auth.spec"
```

Expected: 7 テストすべて PASS

- [ ] **Step 3: Commit する**

```bash
git add apps/habits/e2e/auth.spec.ts
git commit -m "feat(e2e): auth.spec.ts — 認証フローの E2E テストを追加する"
```

---

### Task 4: サインアップ→完了→stash 反映 E2E（`signup-journey.spec.ts`）

**Files:**
- Create: `apps/habits/e2e/signup-journey.spec.ts`

- [ ] **Step 1: `signup-journey.spec.ts` を作成する**

```ts
import { expect, test } from '@playwright/test';

test('サインアップ → タスク完了 → スタッシュに完了数が反映される', async ({ page }) => {
  // テスト実行ごとにユニークなメールアドレスを使い既存ユーザーと衝突しない
  const uniqueEmail = `e2e-${Date.now()}@habits.test`;
  const password = 'habits-e2e-pw';

  // 1. 新規ユーザーとしてサインアップ
  await page.goto('/auth/signup');
  await page.getByLabel('メールアドレス').fill(uniqueEmail);
  await page.getByLabel('パスワード').fill(password);
  await page.getByRole('button', { name: '新規登録' }).click();
  await page.waitForURL('**/today');
  await expect(page.getByRole('heading', { name: '今日のタスク' })).toBeVisible();

  // 2. 初期タスクが表示されていることを確認し、最初のタスクを「完了」にする
  //    auth.users INSERT トリガーで 6 タスク + 2 時間帯が自動生成される
  const completeButton = page.getByRole('button', { name: '完了' }).first();
  await expect(completeButton).toBeVisible({ timeout: 10_000 });
  await completeButton.click();
  await expect(completeButton).toHaveAttribute('aria-pressed', 'true');

  // 3. Supabase へのオプティミスティック書き込みと task_stash トリガーの発火を待つ
  await page.waitForTimeout(2000);

  // 4. スタッシュページに遷移（StashPanel マウント時に refreshTaskStashView() が呼ばれる）
  await page.getByRole('link', { name: 'スタッシュ' }).click();
  await page.waitForURL('**/stash');
  await expect(page.getByRole('heading', { name: 'スタッシュ' })).toBeVisible();

  // 5. 最初のタスクの完了カウントが 1 以上であることを確認
  //    StashRow 構造: <article> → <dl> → <div><dt>完了</dt><dd>1</dd></div>...
  const firstRow = page.locator('article').first();
  await expect(firstRow).toBeVisible({ timeout: 10_000 });
  const completeCount = firstRow.locator('dd').first();
  await expect(completeCount).not.toHaveText('0');
});
```

- [ ] **Step 2: テストを実行する**

```bash
pnpm nx e2e habits -- --project=unauthenticated --grep "signup-journey"
```

Expected: PASS。`dd` の最初の要素が `'1'` を表示する

もし FAIL した場合のデバッグ手順:
- `--headed` オプションを付けてブラウザ表示で確認: `pnpm nx e2e habits -- --headed --grep "signup-journey"`
- スタッシュページのスクリーンショットを確認（`test-results/` に保存される）
- `page.waitForTimeout(2000)` を `3000` に延ばす

- [ ] **Step 3: Commit する**

```bash
git add apps/habits/e2e/signup-journey.spec.ts
git commit -m "feat(e2e): signup-journey.spec.ts — サインアップ→完了→stash E2E を追加する"
```

---

### Task 5: オフライン→再接続→同期 E2E（`offline-sync.spec.ts`）

**Files:**
- Create: `apps/habits/e2e/offline-sync.spec.ts`

このテストは `authenticated` プロジェクト（storageState 使用）で実行される。

- [ ] **Step 1: `offline-sync.spec.ts` を作成する**

```ts
import { expect, test } from '@playwright/test';

test('オフライン中のタスク操作が再接続後に Supabase へ同期される', async ({
  page,
  context,
}) => {
  // 1. 今日のページにアクセス（storageState で認証済み）
  await page.goto('/today');
  await expect(page.getByRole('heading', { name: '今日のタスク' })).toBeVisible();

  // タスクが表示されていることを確認
  const completeButton = page.getByRole('button', { name: '完了' }).first();
  await expect(completeButton).toBeVisible({ timeout: 10_000 });

  // 2. ネットワークをオフラインにする
  //    window の 'offline' イベントが発火し online$.set(false) が呼ばれる
  await context.setOffline(true);

  // 3. オフライン状態でタスクを完了にする（楽観更新: IndexedDB に書き込まれリトライキューに入る）
  await completeButton.click();
  await expect(completeButton).toHaveAttribute('aria-pressed', 'true');

  // 4. スタッシュページに遷移してオフラインバッジが表示されることを確認
  await page.getByRole('link', { name: 'スタッシュ' }).click();
  await page.waitForURL('**/stash');
  // PendingSyncBadge: online$ === false のとき role="status" で表示される
  await expect(page.getByRole('status')).toBeVisible();
  await expect(page.getByText('オフライン')).toBeVisible();

  // 5. オンラインに戻す（legend-state がリトライキューを再送する）
  await context.setOffline(false);

  // 6. オフラインバッジが消えることを確認
  await expect(page.getByRole('status')).not.toBeVisible({ timeout: 5000 });

  // 7. スタッシュを再ロードして同期後の完了カウントを確認
  //    StashPanel マウント時に refreshTaskStashView() が呼ばれ最新データを取得する
  await page.waitForTimeout(2000); // Supabase トリガー発火を待つ
  await page.reload();
  await expect(page.locator('article').first()).toBeVisible({ timeout: 10_000 });
  const completeCount = page.locator('article').first().locator('dd').first();
  await expect(completeCount).not.toHaveText('0');
});
```

- [ ] **Step 2: テストを実行する**

```bash
pnpm nx e2e habits -- --project=authenticated --grep "offline-sync"
```

Expected: PASS

もし FAIL した場合のデバッグ:
- `--headed` でブラウザ表示確認
- `context.setOffline` が期待通り動作しない場合: `page.route('**', r => r.abort())` でリクエストをブロックする代替手段がある
- `page.waitForTimeout(2000)` を `3000` に延ばす

- [ ] **Step 3: 全テストスイートを実行して緑であることを確認する**

```bash
pnpm nx e2e habits
```

Expected: setup + authenticated + unauthenticated の全テスト PASS

- [ ] **Step 4: Commit する**

```bash
git add apps/habits/e2e/offline-sync.spec.ts
git commit -m "feat(e2e): offline-sync.spec.ts — オフライン→再接続→同期 E2E を追加する"
```

---

### Task 6: GitHub Actions CI に Supabase セットアップを追加する

**Files:**
- Modify: `.github/workflows/ci.yml`

- [ ] **Step 1: CI ワークフローを更新する**

`.github/workflows/ci.yml` の `Install Playwright Browsers` ステップの前に以下を追加し、既存の `E2E` ステップに env を付与する:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  actions: read
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - uses: nrwl/nx-set-shas@v4

      - name: Biome (format + lint)
        run: pnpm exec biome ci .

      - name: Typecheck + Unit tests
        run: pnpm nx affected -t typecheck test --parallel=3

      - name: Build
        run: pnpm nx affected -t build --parallel=3

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Start Supabase
        run: supabase start

      - name: Install Playwright Browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: E2E
        run: pnpm nx affected -t e2e
        env:
          # supabase start のデモインスタンスは URL・anon key が固定値
          VITE_SUPABASE_URL: http://localhost:54321
          VITE_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
          E2E_TEST_EMAIL: e2e@habits.test
          E2E_TEST_PASSWORD: habits-e2e-pw
```

- [ ] **Step 2: CI の Supabase セットアップ動作を検証する（PR push で確認）**

ローカルで確認できないため、git push して CI ログを確認する:

```bash
git add .github/workflows/ci.yml
git commit -m "ci: Supabase セットアップと E2E 環境変数を GitHub Actions に追加する"
git push
```

CI ログで以下を確認:
- `supabase start` が正常完了（Docker コンテナ起動）
- `pnpm nx affected -t e2e` が E2E テストを実行
- 全テスト PASS

もし `supabase start` がタイムアウトする場合:
- `supabase start --timeout 300` を試す（デフォルト 120s）

もし E2E が affected に含まれない場合（CI では常時実行したい場合）:
- `pnpm nx run habits:e2e` に変更する

---

## Self-Review チェックリスト

### 1. Spec カバレッジ

| スペック要件 | 対応タスク |
|---|---|
| `pnpm nx e2e habits` で E2E が緑 | Task 1（設定）|
| サインアップ→今日→完了→stash 反映 | Task 4 |
| オフライン→再接続→同期 | Task 5 |
| GitHub Actions CI で E2E 実行 | Task 6 |
| 認証フロー（login/logout/redirect） | Task 3 |

### 2. Placeholder スキャン

なし。すべてのステップに具体的なコードを含む。

### 3. 型一貫性

- `auth.setup.ts` の `authFile` パス（`'e2e/.auth/user.json'`）は `playwright.config.ts` の `storageState` パスと一致
- `context.setOffline` は `BrowserContext` の API（`page.context()` で取得可、テスト引数 `{ context }` でも取得可）
- `aria-pressed` は `StatusButtons` の `<button aria-pressed={isActive}>` と一致

---

**Plan complete and saved to `docs/superpowers/plans/2026-05-18-habits-e2e.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
